<div class="container">
	<% if logged_in? %>

  	<h1 id="user-list">Users</h1>
		<% @list.each do |user| %>
			<%=user.username%>
		<% end %>

	<% else %>

		<h1>Welcome!</h1>
	
	<% end %>
  

</div>
